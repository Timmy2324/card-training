
type InitialStateType = {

}

const initialState: InitialStateType = {

}

type PasswordRecoveryActionType = any;

export const PasswordRecoveryReducer = (state: InitialStateType = initialState, action: PasswordRecoveryActionType) => {
    switch (action.type) {
        default:
            return state;
    }
}